# Release candidate status — 13 September 2026

The source is pushed to the private `codex/initial-build` branch. [The invited live application](https://artwork-proof-agent.vercel.app) and isolated Railway backend are deployed with GPT-5.6 Luna. The local Python suite has 47 passing tests, with PostgreSQL integration checked separately in CI. Linux CI/Docker builds passed. The corrected production frontend passed 12 hosted checks across Chromium, Firefox and WebKit, including real analysis, refresh and approval.

Hosted checks passed for PNG/PDF preflight, spoofed MIME/SVG/malformed-PDF rejection, missing-size clarification without a provider call, denied premature approval, restart persistence, cancellation, reviewer isolation, revocation and the live-disable switch. See `hosting-verification.json`. PostgreSQL is portfolio-only and private-network-only; distinct app roles cannot connect to one another's databases. Backend sleeping, one-vCPU/1-GB limits and readiness checks are configured and verified.

The user requested migrating Artwork to GPT-5.6 Luna on 13 September 2026. **30/30 genuine hosted Luna workflow evaluations passed**, at a conservative set cost of $0.039310. Live execution and frontend invitation entry are enabled with quotas and approvals intact. The OpenAI key is present only in the approved backend; unused Anthropic variables were removed and the process restarted. The original local key files were not changed. No API credit was purchased or plan upgraded. This repository remains private; there is no verified release tag. The empty replay catalogue is explicitly labelled incomplete, not presented as a completed replay demo.

Remaining release work: generate assets with `uv run python tools/replay_assets.py`, render and inspect all six genuine proof PDFs, deploy and verify static replays, and finish the security/publication review. The exporter refuses incomplete sets, missing provider usage or failed safety invariants. Keep Artwork usage within its $2.50 monthly allocation; see [operating commands](HOSTING.md), [actual evaluations](EVALUATIONS.md) and [migration verification](LUNA_MIGRATION.md).
