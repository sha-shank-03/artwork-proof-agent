# Release candidate status — 13 September 2026

The source is pushed to the private `codex/initial-build` branch. [The public shell](https://artwork-proof-agent.vercel.app) and isolated Railway backend are deployed. Deterministic tests, generated OpenAPI types, original artwork samples, responsive UI, proof PDF generation, CI and Docker configuration are implemented. Linux CI/Docker builds passed. The current local suite has 34 passing deterministic tests, including publication-gate refusal checks. Public-shell/mobile/keyboard checks passed on the deployed site in Chromium, Firefox and WebKit.

Hosted checks passed for PNG/PDF preflight, spoofed MIME/SVG/malformed-PDF rejection, missing-size clarification without a provider call, denied premature approval, restart persistence, cancellation, reviewer isolation, revocation and the live-disable switch. See `hosting-verification.json`. The Anthropic key is stored only in the approved Railway backend service. PostgreSQL is new, portfolio-only and private-network-only; distinct app roles cannot connect to one another's databases. Backend sleeping, one-vCPU/1-GB limits and readiness checks are configured and verified.

Incomplete: all 30 real Claude evaluations, genuine public proof replays, the complete Claude-backed hosted workflow and final publication. The supplied Anthropic account has insufficient API credit. Live execution and frontend invitation entry are disabled. No API credit was purchased or plan upgraded. This repository remains private; there is no verified release tag. The empty replay catalogue is explicitly labelled incomplete, not presented as a live demo.

After API credit is available: enable the keyed Railway backend, run the complete hosted evaluation set against its shared budget ledger, fix failures, generate assets with `uv run python tools/replay_assets.py`, render and inspect genuine proof PDFs, enable/deploy the frontend, verify the full live approval flow and finish the security/publication review. The exporter refuses incomplete sets, missing provider usage or failed safety invariants. Keep all model usage within the $2.50 Anthropic monthly allowance; see [operating commands](HOSTING.md).
