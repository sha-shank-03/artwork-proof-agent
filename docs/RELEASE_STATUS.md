# Release candidate status — 13 September 2026

The source is pushed to the private `codex/initial-build` branch. [The public shell](https://artwork-proof-agent.vercel.app) and isolated Railway backend are deployed. Deterministic tests, generated OpenAPI types, original artwork samples, responsive UI, proof PDF generation, CI and Docker configuration are implemented. Linux CI/Docker builds passed. The current local suite has 34 passing deterministic tests, including publication-gate refusal checks. Public-shell/mobile/keyboard checks passed on the deployed site in Chromium, Firefox and WebKit.

Hosted checks passed for PNG/PDF preflight, spoofed MIME/SVG/malformed-PDF rejection, missing-size clarification without a provider call, denied premature approval, restart persistence, cancellation, reviewer isolation, revocation and the live-disable switch. See `hosting-verification.json`. PostgreSQL is portfolio-only and private-network-only; distinct app roles cannot connect to one another's databases. Backend sleeping, one-vCPU/1-GB limits and readiness checks are configured and verified.

The user requested migrating Artwork to GPT-5.6 Luna on 13 September 2026. The prior Anthropic credit error no longer appeared in a subsequent token-count check, but the new provider choice supersedes the Claude rollout. The Luna migration passes 42 deterministic tests locally (PostgreSQL integration runs separately in CI). Hosted Luna verification is in progress. Live execution and frontend invitation entry stay disabled until that verification passes. No API credit was purchased or plan upgraded. This repository remains private; there is no verified release tag. An empty replay catalogue is explicitly labelled incomplete, not presented as a completed replay demo.

Remaining release work: run the complete hosted Luna evaluation set against its shared budget ledger, fix failures, generate assets with `uv run python tools/replay_assets.py`, render and inspect genuine proof PDFs, enable/deploy the frontend, verify the full live approval flow and finish the security/publication review. The exporter refuses incomplete sets, missing provider usage or failed safety invariants. Keep Artwork usage within its $2.50 monthly allocation; see [operating commands](HOSTING.md) and [migration verification](LUNA_MIGRATION.md).
